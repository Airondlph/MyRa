package MyRa.data;


/**
 * Symbolic interface that indicates that the class/object is immutable (data cannot be modified, only read (only getters)).
 * @author airon
 */
public interface Immutable {}
